#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
script="${script_dir}/fetchmd.cjs"

if [ ! -f "${script}" ]; then
  echo "Error: missing built script: ${script}" >&2
  exit 2
fi

exec node "${script}" "$@"
