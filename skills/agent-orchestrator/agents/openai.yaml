interface:
  display_name: "Agent Orchestrator"
  short_description: "Orchestrate multi-agent work"
  default_prompt: "Use $agent-orchestrator to coordinate a phase-gated multi-agent engineering loop. Assume the standard repo agent roles are available."
